#include "caction.h"

Caction::Caction()
{
    x = A0;
}

Caction::~Caction()
{

}

void Caction::InitBall()
{
	/*
	fita =		0;
	w0 =		sqrt( k / m );
	w =			w0;
	T =			2 * pi1 * w0;
	AtimeT =	A0 * cos(w * T + fita);
	ldekrem =	log(A0 / AtimeT);
	oldx =		A0;
	oldtime =	-1;
	v =			0;
	E0 =		k * A0 * A0 / 2;
	E =			E0;
	*/
}
